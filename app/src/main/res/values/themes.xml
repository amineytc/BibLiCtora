<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.BibLiCtora" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.BibLiCtora" parent="Base.Theme.BibLiCtora">
        <item name="android:statusBarColor">@color/black</item>
    </style>

</resources>